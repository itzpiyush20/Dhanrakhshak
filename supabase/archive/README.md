# Archived migrations

These files are historical record only — every change they made has been folded into
the single authoritative `supabase/schema.sql`. Do not run them against a fresh
project; running `supabase/schema.sql` alone is sufficient.

They are kept here purely so the schema's evolution is traceable during due diligence.
